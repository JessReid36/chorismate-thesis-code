# 08_relaxed_validation — Tier 2 (relaxed-path validation, NOT claimed novel)

For the top ~3-5 candidates ranked by the Tier-1 certified screen (05_optimise): re-optimise
the path/TS UNDER the designed charge field to get the real catalytic effect and confirm a
connected R->P path (TS allowed to shift). Inherits the Route-1 / surrogate / charge-attenuation
machinery, since bare point charges collapse the path. Headline validation = ranking preservation
(does the frozen-path screen order survive relaxation?). See ../DESIGN_DECISIONS.md.
